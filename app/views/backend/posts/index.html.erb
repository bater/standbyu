<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>City</th>
      <th>District</th>
      <th>Adress</th>
      <th>Phone</th>
      <th>Service time</th>
      <th>Tag</th>
      <th>Description</th>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.city %></td>
        <td><%= post.district %></td>
        <td><%= post.adress %></td>
        <td><%= post.phone %></td>
        <td><%= post.service_time %></td>
        <td><%= post.tag %></td>
        <td><%= post.description %></td>
        <td><%= post.url %></td>
        <td><%= link_to 'Show', [:backend, post] %></td>
        <td><%= link_to 'Edit', edit_backend_post_path(post) %></td>
        <td><%= link_to 'Destroy', [:backend, post], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_backend_post_path %>
